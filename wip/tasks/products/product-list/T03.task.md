---
title: 'T03 — Implementar ProductService'
type: task
id: 'T03'
scope: 'products'
task-list: 'wip/tasks/products/product-list/product-list.task-list.md'
status: pending
created: '2026-05-23'
updated: '2026-05-23'
branch: ''
commit: ''
---

## Descrição

Criar o `ProductService` com `providedIn: 'root'`, gerenciando estado via `signal()` e expondo o método `loadProducts()` que consome `GET /products`.

## Contexto

**Tech spec referência:** seção "Serviços"  
**ACs cobertos:** TAC01, TAC02, TAC03

## Critérios de Conclusão (DoD)

- [ ] Código implementado conforme a tech spec
- [ ] Sem erros de lint/prettier (`yarn lint-staged`)
- [ ] Commit feito com mensagem conventional

## Subtarefas

- [ ] 1. Criar `src/app/core/services/product.service.ts`
- [ ] 2. Declarar signals: `products = signal<Product[]>([])`, `loading = signal(false)`, `error = signal<string | null>(null)`
- [ ] 3. Implementar `loadProducts()` com `HttpClient.get`, `catchError` e `finalize`
- [ ] 4. Injetar `HttpClient` via `inject(HttpClient)`
- [ ] 5. Usar `environment.apiUrl` como base da URL

## Arquivos Afetados

| Arquivo                                    | Ação  |
| ------------------------------------------ | ----- |
| `src/app/core/services/product.service.ts` | criar |

## Testes a Escrever

Cobertos na T04.

## Bloqueios

Depende de: T01 (environment), T02 (model)

## Notas de Implementação

-
