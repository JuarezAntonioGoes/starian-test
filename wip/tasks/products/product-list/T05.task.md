---
title: 'T05 — Configurar rota /products em app.routes.ts'
type: task
id: 'T05'
scope: 'products'
task-list: 'wip/tasks/products/product-list/product-list.task-list.md'
status: in-progress
created: '2026-05-23'
updated: '2026-05-24'
branch: ''
commit: ''
---

## Descrição

Registrar a rota `/products` em `app.routes.ts` apontando para `ProductListComponent` com lazy loading via `loadComponent`.

## Contexto

**Tech spec referência:** seção "Componentes" (rota) e "Decisões Técnicas"  
**ACs cobertos:** TAC05 (pré-requisito — componente precisa ser acessível pela rota)

## Critérios de Conclusão (DoD)

- [ ] Código implementado conforme a tech spec
- [ ] Sem erros de lint/prettier (`yarn lint-staged`)
- [ ] Commit feito com mensagem conventional

## Subtarefas

- [ ] 1. Abrir `src/app/app.routes.ts`
- [ ] 2. Adicionar rota `{ path: 'products', loadComponent: () => import(...).then(m => m.ProductListComponent) }`
- [ ] 3. Adicionar redirect `{ path: '', redirectTo: 'products', pathMatch: 'full' }` se não existir
- [ ] 4. Verificar que `provideRouter(routes)` está configurado em `app.config.ts`

## Arquivos Afetados

| Arquivo                 | Ação      |
| ----------------------- | --------- |
| `src/app/app.routes.ts` | modificar |
| `src/app/app.config.ts` | verificar |

## Testes a Escrever

Não aplicável — configuração de rota; cobertura via testes do componente (T07).

## Bloqueios

Depende de: T06 (ProductListComponent precisa existir para a rota funcionar)

## Notas de Implementação

-
